name: restore-hours-page
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/restore-hours-page.yml'
jobs:
  restore:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Restore
        run: echo starting
